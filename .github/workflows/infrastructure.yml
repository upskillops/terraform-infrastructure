name: Hello Infra Runner

on: 
  push

jobs:
   hello:
       runs-on: [ infra ]
       steps:
          - name: Print Dev Secret
            run:  echo ${{ secrets.DEV_CLIENT_ID }}
          - name: Print Prod Secret
            run:  echo ${{ secrets.PROD_CLIENT_ID }}

            
