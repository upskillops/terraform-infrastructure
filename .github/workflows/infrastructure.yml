name: Hello Infra Runner

on: 
  push

jobs:
   build:
     runs-on: [infra]
     steps:
        - name: Build Job
          run: echo1 "I am in build phase"
        - name: Build Secod Job
          run: echo "Runing Second Build Job" 

   test:
      runs-on: [infra]
      needs: build
      steps:
        - name: Test Job
          run: echo "Running Test Job"

   deploy:
      runs-on: [infra]
      needs: [build,test]
      steps:
        - name: Deploy Job
          run: echo "Running Deploy Job"

            
