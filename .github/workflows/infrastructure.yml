name: Hello Infra Runner

on: 
  push

jobs:
   build-java:
     runs-on: [infra]
     steps:
        - name: Build Java Application
          run: echo "I am in build phase" > app.txt
   
   read-Code:
       runs-on: [infra]
       needs: [build-java]
       steps:
          - name: Read the File
            run: cat aap.txt
     

            
